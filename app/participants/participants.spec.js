'use strict';

describe('myApp.participants module', function() {

  beforeEach(module('myApp.participants'));

  describe('participants controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var participantsCtrl = $controller('ParticipantsCtrl');
      expect(participantsCtrl).toBeDefined();
    }));

  });
});
