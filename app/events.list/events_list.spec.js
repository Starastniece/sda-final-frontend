'use strict';

describe('myApp.events_list module', function() {

  beforeEach(module('myApp.events_list'));

  describe('events_list controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var eventsListCtrl = $controller('EventsListCtrl');
      expect(eventsListCtrl).toBeDefined();
    }));

  });
});
