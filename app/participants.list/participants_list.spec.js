'use strict';

describe('myApp.participants_list module', function() {

  beforeEach(module('myApp.participants_list'));

  describe('ParticipantsListCtrl controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var participantsListCtrl = $controller('ParticipantsListCtrl');
      expect(participantsListCtrl).toBeDefined();
    }));

  });
});
