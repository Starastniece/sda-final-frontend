'use strict';

describe('myApp.attendants module', function() {

  beforeEach(module('myApp.attendants'));

  describe('attendants controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var attendantsCtrl = $controller('AttendantsCtrl');
      expect(attendantsCtrl).toBeDefined();
    }));

  });
});
